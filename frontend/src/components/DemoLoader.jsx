function DemoLoader() {
  return (
    <button
      style={{
        padding: "12px 20px",
        border: "none",
        borderRadius: "8px",
        cursor: "pointer"
      }}
    >
      Load Executive Demo
    </button>
  );
}

export default DemoLoader;